---
description: One disciplined pass to keep this PR merge-ready — triage unresolved threads, resolve merge conflicts, fix failing CI.
---

Load and follow the `autopilot` skill.

Focus hints from the requester: "$ARGUMENTS"
